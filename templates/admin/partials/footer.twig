<footer class="bg-white dark:bg-dark-bg-secondary border-t border-gray-200 dark:border-gray-700">
    <div class="container mx-auto px-4 sm:px-6 lg:px-8">
        <div class="py-4 flex items-center justify-between">
            <div class="text-sm text-gray-500 dark:text-gray-400">
                &copy; {{ "now"|date("Y") }} {{ app_name|default('Admin Panel') }}. All rights reserved.
            </div>
            <div class="flex items-center space-x-4 text-sm text-gray-500 dark:text-gray-400">
                <span>Version {{ app_version|default('1.0.0') }}</span>
                <a href="{{ url_for('admin.help') }}" class="hover:text-gray-700 dark:hover:text-gray-300">
                    <i class="fas fa-question-circle mr-1"></i> Help
                </a>
            </div>
        </div>
    </div>
</footer>