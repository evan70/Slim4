<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin - {{ title }}</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100">
    <div class="flex">
        <!-- Sidebar -->
        <div class="w-64 h-screen bg-gray-800">
            <div class="flex items-center justify-center h-16 bg-gray-900">
                <span class="text-white text-xl font-semibold">Admin Panel</span>
            </div>
            <nav class="mt-4">
                <a href="{{ url_for('admin.dashboard') }}" class="block px-4 py-2 text-gray-300 hover:bg-gray-700">Dashboard</a>
                <a href="{{ url_for('admin.users') }}" class="block px-4 py-2 text-gray-300 hover:bg-gray-700">Users</a>
            </nav>
        </div>
        
        <!-- Content -->
        <div class="flex-1 p-8">
            {% block content %}{% endblock %}
        </div>
    </div>
</body>
</html>
